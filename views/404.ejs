<%- include('partials/head', { title: t('404.titel') }) %>

<section class="page-head">
  <div class="container narrow">
    <h1 class="page-title"><%= t('404.titel') %></h1>
    <p class="page-lead">
      <%= t('404.lead') %>
      <a href="<%= url('/') %>"><%= t('404.terug') %></a>.
    </p>
  </div>
</section>

<%- include('partials/foot') %>
